# Rendered output -- large binaries, regenerate with pnpm render:episode
out/

# Captured screen recordings live here but are not committed. They are large,
# they change every reshoot, and git is the wrong store for them.
public/*.mp4
public/*.mov
public/*.webm

# Thumbnail source frames, pulled out of the footage with ffmpeg. Derived,
# like the footage itself:
#   ffmpeg -ss 45 -i public/ep01.mp4 -frames:v 1 public/shot-organizer.png
public/shot-*.png
